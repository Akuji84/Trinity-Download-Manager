# Trinity Download Manager

Professional desktop download manager built with Tauri, Rust, React, and TypeScript.

## Development

Install dependencies:

```bash
npm install
```

Run the desktop app in development:

```bash
npm run tauri dev
```

Build the frontend:

```bash
npm run build
```

Run the standalone download behavior test harness:

```bash
npm run harness
```

See `AI_HANDOFF.md` for product scope, roadmap, decisions, and progress notes.
