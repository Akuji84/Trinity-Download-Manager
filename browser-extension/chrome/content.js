// Browser-managed download takeover is the primary path.
// Keep the content script loaded so we can reintroduce a narrow fallback later
// without changing the manifest again, but do not intercept page clicks here.
